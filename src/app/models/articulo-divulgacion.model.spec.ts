import { ArticuloDivulgacion } from './articulo-divulgacion.model';

describe('ArticuloDivulgacion', () => {
  it('should create an instance', () => {
    expect(new ArticuloDivulgacion()).toBeTruthy();
  });
});
