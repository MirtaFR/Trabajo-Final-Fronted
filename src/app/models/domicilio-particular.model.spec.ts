import { DomicilioParticular } from './domicilio-particular.model';

describe('DomicilioParticular', () => {
  it('should create an instance', () => {
    expect(new DomicilioParticular()).toBeTruthy();
  });
});
