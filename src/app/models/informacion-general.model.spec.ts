import { InformacionGeneral } from './informacion-general.model';

describe('InformacionGeneral', () => {
  it('should create an instance', () => {
    expect(new InformacionGeneral()).toBeTruthy();
  });
});
