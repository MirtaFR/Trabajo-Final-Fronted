import { RecuperacionPassword } from './recuperacion-password.model';

describe('RecuperacionPassword', () => {
  it('should create an instance', () => {
    expect(new RecuperacionPassword()).toBeTruthy();
  });
});
